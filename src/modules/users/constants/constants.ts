const userURL = 'user';
const userNotFoundMessage = 'user with this id not found';
const incorrectBodyMessage = 'old password is incorrect';

export default {
  userURL,
  userNotFoundMessage,
  incorrectBodyMessage,
};
